- name: Destroy lightsail VMs
  hosts: localhost
  vars_files:
    - vars.yml
  tasks:
    - name: Destroy lightsail VMs
      lightsail:
        state: absent
        name: "{{ item }}"
        region: "{{ vm.aws.region }}"
      with_items: "{{ vm.hosts }}"
